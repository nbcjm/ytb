var https = require('https');
var fs = require('fs');
var url = require('url');
var path = require('path');

var app = https.createServer(function (request,response){
    var musicurl = request.url;

})